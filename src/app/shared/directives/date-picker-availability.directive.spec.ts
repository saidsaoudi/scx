import { DatePickerAvailabilityDirective } from './date-picker-availability.directive';

describe('DatePickerAvailabilityDirective', () => {
  it('should create an instance', () => {
    const directive = new DatePickerAvailabilityDirective();
    expect(directive).toBeTruthy();
  });
});
